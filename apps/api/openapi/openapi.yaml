openapi: 3.0.3
info:
  title: ProSlides API
  version: 0.1.0
  description: Contract-first API for the Go modular monolith.
servers:
  - url: /
paths:
  /healthz:
    get:
      operationId: getHealth
      summary: Process liveness probe.
      responses:
        '200':
          description: The API process can serve requests. This endpoint does not check dependencies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
  /readyz:
    get:
      operationId: getReadiness
      summary: Required dependency readiness probe.
      responses:
        '200':
          description: PostgreSQL and Redis are reachable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Readiness'
        '503':
          description: At least one required dependency is unavailable or not configured. Driver errors and connection details are never returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Readiness'
  /api/v1/version:
    get:
      operationId: getVersion
      responses:
        '200':
          description: API version
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Version'
  /api/v1/auth/register:
    post:
      operationId: registerAccount
      summary: Create an account and start an authenticated session.
      description: Sets an HttpOnly session cookie and a readable CSRF cookie. Passwords are never returned or logged.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Account created and session established.
          headers:
            Set-Cookie:
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentUser'
        '400': { $ref: '#/components/responses/ValidationError' }
        '409':
          description: An account already uses this email address.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/v1/auth/login:
    post:
      operationId: loginAccount
      summary: Start an authenticated session.
      description: Sets an HttpOnly session cookie and a readable CSRF cookie.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Session established.
          headers:
            Set-Cookie:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CurrentUser' }
        '400': { $ref: '#/components/responses/ValidationError' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /api/v1/auth/logout:
    post:
      operationId: logoutAccount
      summary: End the current authenticated session.
      description: Requires the session cookie and `X-CSRF-Token` equal to the CSRF cookie value.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Session ended, whether or not it had already expired. }
        '403': { $ref: '#/components/responses/CSRFError' }
  /api/v1/auth/me:
    get:
      operationId: getCurrentUser
      summary: Return the account represented by the current session cookie.
      responses:
        '200':
          description: Current authenticated account.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CurrentUser' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /api/v1/presentations/{presentationId}:
    get:
      operationId: getPresentation
      summary: Return one presentation and its ordered slides for its owner.
      description: Requires the current authenticated session. Returns 404 when the presentation does not exist or is not owned by the current account, so identifiers are not disclosed.
      parameters:
        - $ref: '#/components/parameters/PresentationId'
      responses:
        '200':
          description: The presentation with slides ordered by ascending position.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Presentation' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/presentations:
    post:
      operationId: createPresentation
      summary: Create an empty presentation owned by the current account.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreatePresentationRequest' }
      responses:
        '201':
          description: Presentation created.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Presentation' }
        '400': { $ref: '#/components/responses/ValidationError' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/CSRFError' }
  /api/v1/presentations/{presentationId}/slides:
    post:
      operationId: createSlide
      summary: Add a slide to an owned presentation.
      description: Requires an authenticated owner and a CSRF token. Returns 404 for absent or non-owned presentations.
      parameters:
        - $ref: '#/components/parameters/PresentationId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateSlideRequest' }
      responses:
        '201':
          description: Slide created.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Slide' }
        '400': { $ref: '#/components/responses/ValidationError' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/CSRFError' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/live/sessions/{sessionId}/events:
    get:
      operationId: subscribeLiveSession
      summary: Subscribe to server-sent live events.
      parameters:
        - $ref: '#/components/parameters/SessionId'
        - name: Last-Event-ID
          in: header
          required: false
          schema:
            type: string
      responses:
        '200':
          description: text/event-stream response
          content:
            text/event-stream:
              schema:
                type: string
components:
  parameters:
    CSRFToken:
      name: X-CSRF-Token
      in: header
      required: true
      schema: { type: string }
    SessionId:
      name: sessionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    PresentationId:
      name: presentationId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  schemas:
    RegisterRequest:
      type: object
      required: [email, display_name, password]
      properties:
        email: { type: string, format: email, maxLength: 320 }
        display_name: { type: string, minLength: 1, maxLength: 100 }
        password: { type: string, format: password, minLength: 12, maxLength: 128 }
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email, maxLength: 320 }
        password: { type: string, format: password, maxLength: 128 }
    CurrentUser:
      type: object
      required: [id, email, display_name]
      properties:
        id: { type: string, format: uuid }
        email: { type: string, format: email }
        display_name: { type: string }
    Presentation:
      type: object
      required: [id, title, slides]
      properties:
        id: { type: string, format: uuid }
        title: { type: string, minLength: 1, maxLength: 500 }
        slides:
          type: array
          items: { $ref: '#/components/schemas/Slide' }
    CreatePresentationRequest:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1, maxLength: 500 }
    CreateSlideRequest:
      type: object
      required: [position, kind, content]
      properties:
        position: { type: integer, minimum: 0 }
        kind: { type: string, minLength: 1, maxLength: 100 }
        content: { type: object, additionalProperties: true }
    Slide:
      type: object
      required: [id, position, kind, content]
      properties:
        id: { type: string, format: uuid }
        position: { type: integer, minimum: 0 }
        kind: { type: string, minLength: 1, maxLength: 100 }
        content:
          type: object
          additionalProperties: true
    Error:
      type: object
      required: [error]
      properties:
        error: { type: string }
    Health:
      type: object
      required: [status]
      properties:
        status:
          type: string
          example: ok
    Readiness:
      type: object
      required: [status, dependencies]
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        dependencies:
          type: object
          additionalProperties:
            type: string
            enum: [ok, unavailable]
    Version:
      type: object
      required: [service, environment, api_version]
      properties:
        service: { type: string }
        environment: { type: string }
        api_version: { type: string }
  responses:
    ValidationError:
      description: Request validation failed.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    Unauthorized:
      description: No valid current session exists.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    CSRFError:
      description: The CSRF header and cookie do not match a valid current session.
      content:
        application/json:
              schema: { $ref: '#/components/schemas/Error' }
    NotFound:
      description: The requested resource does not exist or is not visible to the current account.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
