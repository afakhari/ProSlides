# گزارش بررسی فرایند پرزنتیشن

تاریخ بررسی: 2026-02-13

## هدف
بررسی پایداری مسیر پرزنتیشن از دید بک‌اند و فرانت‌اند با تمرکز روی سناریوهای کلیدی:

- اعتبارسنجی و Resolve شدن Access Code
- شروع/مدیریت سشن بازیکن‌ها
- دریافت نتایج سوال و لیدربورد
- ساخت و Route شدن صفحات پرزنتیشن در Build فرانت‌اند

## چک‌های اجرا شده

1. نصب وابستگی‌های بک‌اند با `uv sync`
2. اجرای کامل تست‌های بک‌اند:
   - `PYTHONPATH=/workspace/ProSlides uv run pytest backend/srvs/office/tests -q`
3. نصب وابستگی‌های فرانت‌اند:
   - `npm --prefix frontend ci`
4. بررسی کیفیت کد فرانت‌اند:
   - `npm --prefix frontend run lint`
5. بررسی Build فرانت‌اند (برای اطمینان از صحت Route/Bundle پرزنتیشن):
   - `npm --prefix frontend run build`

## نتیجه

- تمام 115 تست بک‌اند پاس شدند.
- lint فرانت‌اند پاس شد.
- build فرانت‌اند با موفقیت انجام شد و باندل `PresentationEntry` تولید شد.
- هشدار اندازه چانک در build وجود دارد، اما خطای اجرایی نیست.

## سناریوهای پوشش‌داده‌شده (نمونه)

- اعتبار Access Code و resolve به quiz/session
- مدیریت Session بازیکنان
- دریافت Leaderboard (حالت‌های موفق و خطا)
- دریافت Question Results (حالت‌های موفق و خطا)
- اعتبارسنجی ترتیب اسلایدها و محدودیت نوع اسلایدها
- reset نتیجه‌ی کوییز

این سناریوها با اتکا به تست‌های موجود در `backend/srvs/office/tests/` تایید شده‌اند.
